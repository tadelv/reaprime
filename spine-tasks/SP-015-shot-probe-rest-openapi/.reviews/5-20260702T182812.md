### Verdict: REVISE

### Summary

The changes successfully expose `probeTemperature` on the REST endpoints by updating the OpenAPI spec, documentation, and handler tests as requested. The handler already serializes this field correctly. However, a new unused import was introduced in the test file, which causes `flutter analyze` to fail.

### Build / typecheck

Ran `flutter analyze`:
```
warning • Unused import: 'package:reaprime/src/controllers/scale_controller.dart'. Try removing the import directive • test/webserver/shots_handler_test.dart:7:8 • unused_import
410 issues found. (ran in 13.2s)
Command exited with code 1
```

### Blocking issues

1. **`test/webserver/shots_handler_test.dart:7`** — Unused import of `package:reaprime/src/controllers/scale_controller.dart` causes the typecheck/analyzer to fail.

```json
{"verdict":"REVISE","feedback":"Remove unused import in shots_handler_test.dart to pass flutter analyze."}
```
