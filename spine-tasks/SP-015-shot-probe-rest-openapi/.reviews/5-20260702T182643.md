### Verdict: REVISE

### Summary
The changes successfully expose `probeTemperature` on shot REST endpoints in the OpenAPI spec and documentation. The handler tests correctly verify that the values are returned, taking advantage of existing serialization. However, an unused import was introduced in the test file which causes the `flutter analyze` typecheck to fail.

### Build / typecheck
`flutter test test/webserver/shots_handler_test.dart` passed successfully.
`flutter analyze` failed with:
```text
warning • Unused import: 'package:reaprime/src/controllers/scale_controller.dart'. Try removing the import directive • test/webserver/shots_handler_test.dart:7:8 • unused_import
```

### Blocking issues
1. **`test/webserver/shots_handler_test.dart:7`** — Contains an unused import (`import 'package:reaprime/src/controllers/scale_controller.dart';`) that causes the `flutter analyze` typecheck to fail. Please remove it.

```json
{
  "verdict": "REVISE",
  "feedback": "The OpenAPI spec and docs were updated correctly and the tests accurately verify the JSON output. However, the newly introduced unused import in test/webserver/shots_handler_test.dart fails the flutter analyze typecheck. Please remove the unused import."
}
```
