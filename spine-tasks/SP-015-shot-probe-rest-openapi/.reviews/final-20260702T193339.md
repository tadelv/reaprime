### Verdict: PASS

### Summary
The completion criteria have been successfully met. `probeTemperature` was correctly added to the `ShotSnapshot` in the OpenAPI schema (`assets/api/rest_v1.yml`) and `doc/Api.md`. Tests were also updated to verify the REST JSON payloads, and they pass successfully.

```json
{"verdict":"PASS","feedback":"All completion criteria are met and tests pass. The OpenAPI spec, docs, and tests have been correctly updated to expose `probeTemperature`."}
```